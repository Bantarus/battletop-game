export class Card {

    // the card ID
    id: bigint;


    // the owner id 
    ownerId: bigint;

    
    // the card genetic ( equivalent to class/role in rpg )
    genetic: bigint;

    // card energy origin
    origin: bigint;

    // card alignment
    alignment: number;


    // card reunited flag 
    reunited: boolean;

    // others combat characteristics ( modifiers from 0 to 5 )

    
 
  }